<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Buscar cliente</title>
  <link rel="stylesheet" href="styles.css">
  <style>
    .button {
      display: inline-block;
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      text-align: center;
      text-decoration: none;
      font-size: 16px;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <h1>Buscar cliente</h1>
  <form method="get" action="/clientes/buscar">
    <label for="rut">RUT:</label>
    <input type="text" id="rut" name="rut">
    <button type="submit">Buscar</button>
  </form>
  {{#if clienteEncontrado}}
    <h2>Cliente encontrado</h2>
    <table>
      <tr>
        <th>RUT</th>
        <th>Nombre</th>
        <th>Edad</th>
      </tr>
      <tr>
        <td>{{clienteEncontrado.rut}}</td>
        <td>{{clienteEncontrado.nombre}}</td>
        <td>{{clienteEncontrado.edad}}</td>
      </tr>
    </table>
    <a href="/" class="button">Volver a la lista de clientes</a>
  {{/if}}
</body>
</html>
